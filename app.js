let arr = ["Firdavs","Amir","Davron",`Zioy`];

alert("sizning arryngizga ${arr.length}ta element bor");

let salom = confirm(`malumot o'chrasizmi`);

if(salom) {
    arr.pop();
}else{
    arr.pop(jasur)
}

alert("siznig arraligizda ${arr.length}ta elemet bor");